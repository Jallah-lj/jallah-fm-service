services:
  postgres:
    image: postgres:alpine
    environment:
      POSTGRES_PASSWORD: password 
    ports:
      - 5433:5433
    volumes:
      - postgres:/var/lib/postgresql/data

volumes:
  postgres:
